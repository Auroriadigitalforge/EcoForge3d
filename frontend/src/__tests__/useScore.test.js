// Feature: eco-forge-3d
// Unit tests for useScore hook
// TODO: implement with @testing-library/react renderHook
import { describe, it } from 'vitest';

describe('useScore hook', () => {
  it.todo('initializes score from localStorage on mount');
  it.todo('setScore persists to localStorage and triggers re-render with new value');
  it.todo('initializes to 50 when localStorage is empty');
});
